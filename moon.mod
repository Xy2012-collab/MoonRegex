name = "X1119/MoonRegex"
version = "0.1.0"
readme = "README.mbt.md"
repository = "https://gitlink.org.cn/X1119/MoonRegex"
license = "Apache-2.0"
keywords = ["regex", "regular-expression", "pattern", "match", "parser"]
description = "纯 MoonBit 正则表达式引擎，基于 AST 编译+递归回溯匹配，支持 .*+?|()[] [a-z] \\d\\w\\s {n,m} 全语法，零依赖"
